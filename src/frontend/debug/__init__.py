"""Developer affordances for iterating on Phase 1 UI screens.

This package is **dev-only**. Nothing under ``frontend/debug/`` is
imported by ``frontend.main`` or by any production code path. The
debug entry point is a separate command:

    python -m frontend.debug.main

See ``frontend/debug/README.md`` for usage.
"""
